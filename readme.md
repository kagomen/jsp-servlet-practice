```bash
# build
javac -cp "/opt/homebrew/Cellar/tomcat/11.0.6/libexec/lib/servlet-api.jar" WEB-INF/classes/First.java

# tomcat start
catalina run
```

```bash
chmod +x start-dev.sh

./start-dev.sh First
```
